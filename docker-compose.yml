version: "3"

services:
  app:
    build: .
    env_file: - .env

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - appversion: "3"

services:
  app:
    build: .
    ports:
      - "3000:3000"
volumes:
  app-data:
